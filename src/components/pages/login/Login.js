import LoginForm from "./LoginForm";

export const Login = () => {
	return (
		<div
			className={
				"flex flex-grow items-center justify-center flex-col transition-all"
			}
		>
			<LoginForm />
		</div>
	);
};
